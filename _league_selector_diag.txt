=== HTML id="leagueImportPreference" ===
index=216719

div></div>
 <nav class="snav">
  <button class="nav active" data-page="news"><span class="navIcon"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 5.5h16v13H4zM8 9h8M8 13h8M8 17h5"/></svg></span><span>News</span></button>
  <button class="nav" data-page="team"><span class="navIcon"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 4.5 5 6l-2 4 3 1.5V20h12v-8.5l3-1.5-2-4-3-1.5c-.8 1.4-2.1 2.1-4 2.1S8.8 5.9 8 4.5Z"/></svg></span><span>My Team</span></button>
  <button class="nav" data-page="transfers"><span class="navIcon"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M5 8h13m0 0-3-3m3 3-3 3M19 16H6m0 0 3 3m-3-3 3-3"/></svg></span><span>Transfers</span></button>
  <button class="nav" data-page="leagues"><span class="navIcon"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 4h8v4c0 3-1.7 5-4 5s-4-2-4-5V4Zm0 2H4c0 3 1.4 5 4 5m8-5h4c0 3-1.4 5-4 5m-4 2v4m-4 3h8m-6-3h4"/></svg></span><span>Leagues</span></button>
  <button class="nav" data-page="star"><span class="navIcon"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="m12 3 2.6 5.4 6 .9-4.3 4.2 1 6-5.3-2.8-5.3 2.8 1-6-4.3-4.2 6-.9L12 3Z"/></svg></span><span>Star XI</span></button>
  <button class="nav" data-page="table"><span class="navIcon"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 4h16v16H4zM4 9h16M4 14h16M9 4v16"/></svg></span><span>League Table</span></button>
  <button class="nav subnav" data-page="fixtures"><span class="navIcon"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M5 5h14v15H5zM8 3v4m8-4v4M5 9h14M9 13h2m3 0h2m-7 3h2m3 0h2"/></svg></span><span>Fixtures &amp;
 Results</span></button>
  <button class="nav" data-page="stats"><span class="navIcon"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M5 20V9h4v11M10 20V4h4v16m1 0v-7h4v7"/></svg></span><span>Stats</span></button>
  <button class="nav" data-page="rules"><span class="navIcon"><svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M9.8 9a2.4 2.4 0 1 1 3.2 2.3c-.8.3-1 1-1 1.7M12 17h.01"/></svg></span><span>Rules</span></button>
 </nav>

 <div class="syncbox"><small>FM Save Sync</small><div class="syncBtns"><button id="seasonImportBtn" class="creatorOnly syncBtn primary">Season / Database Import</button><button id="updateImportBtn" class="creatorOnly syncBtn">Gameweek Update Import</button><button id="exportDebugBtn" class="syncBtn debug">Export Debug</button><button id="clearDatabaseBtn" class="creatorOnly syncBtn danger">Clear Database</button></div><select id="leagueImportPreference" class="syncLeague" title="Manual choice is authoritative and will not be overridden by retained match data"><option value="">League: Auto detect</option><option value="Premier League">League: Premier League</option><option value="EFL Championship">League: EFL Championship</option></select><input id="seasonImportFile" type="file" accept=".fm" hidden><input id="updateImportFile" type="file" accept=".fm" hidden><div id="syncProgress" class="syncProgress"><div class="syncProgressTop"><span id="syncProgressLabel">Waiting…</span><b id="syncProgressPct">0%</b></div><div class="syncTrack"><div id="syncProgressFill" class="syncFill"></div></div></div><div id="syncStatus" class="syncStatus">No FM save imported yet.</div></div>
 <div class="gwbox"><small>Save status</small><div id="sideGW" class="gwbig">Next GW 5</div><div class="siderow"><span>FM through</span><strong id="sideCompleted">GW 4</strong></div><div class="siderow"><span>Team</span><strong id="sideTeam">Not entered</strong></div><div class="siderow"><span>Transfers</span><strong id="sideFT">Unlimited</strong></div><div class="siderow"><span>Total points</span><strong id="sidePts">0</strong></div></div>
</aside>
<main class="main">
 <header class="top"><div><h1 id="pageTitle">News</h1><div id="pageSub" class="topSub">Transfers, market movements and player availability.</div></div><div class="user"><span id="cloudBadge" class="cloudBadge"><i class="cloudDot"></i><span id="cloudUser">Offline</span></span><div class="userAvatar">G</div><div><b>The Gaffer</b><div id="competitionTopLabel" class="topSub">FM Fantasy</div></div></div></header>

 <section id="transfersPage" class="page">
  <div class="summary"><div class="sum"><small>Budget</small><b>£100.0m</b></div><div class="sum good"><small>In Bank</small><b id="bank">£100.0m</b></div><div class="sum"><small>Players</small><b id="squadCount">0 / 15</b></div><div class="sum"><small>Free Transfers</small><b id="freeTransfers">Unlimited</b></div><div id="transferCostCard" class="sum warn"><small>Transfer Cost</small><b id="transferCost">Initial squad</b></div></div>
  <div class="split">
   <div class="card pitchCard"><div class="cardh"><div><h2>Squad Selection</h2><div class="muted">2 GK · 5 DEF · 5 MID · 3 FWD · max 3 from one club.</div></div><button id="autoFill" class="btn dark">✦ Auto Fill Remaining</button></div><div class="pitchScroll"><div class="pitch"><div class="halfPitchMarkings"><svg class="outline pitchOutline" viewBox="0 0 1000 1000" preserveAspectRatio="none" aria-hidden="true"><path d="M 0 470 L 93 0 L 897 0 L 1000 470"></path><path class="cornerArc" d="M 85 38 Q 120 34 120 0 M 870 0 Q 870 34 905 38"></path></svg><i class="penalty"></i><i class="six"></i><i class="goal"></i></div><div class="centerCircle"></div><div id="txGK" class="pline gk"></div><div id="txDEF" class="pline def"></div><div id="txMID" class="pline mid"></div><div id="txFWD" class="pline fwd"></div></div></div><div id="transferRule" class="rulebar bad"></div><div class="actionbar"><div><b id="transferHeadline">Initial squad</b><div id="transferHelp" class="muted">Unlimited changes until your first scoring Gameweek has been played.</div></div><div><button id="wildcard" class="btn dark" style="display:none">Wildcard</button> <button id="confirmTransfers" class="btn pink" style="display:none">Confirm Transfers</button></div></div><div class="actionbar"><div class="transferDraftActions"><button id="clearDraft" class="btn dark">Clear Draft</button><button id="restoreUnreplaced" class="btn dark" disabled>Restore Empty Slots</button><button id="revertTransfers" class="btn dark" disabled>Reset All Transfers</button></div><button class="btn pink" data-go="team">Manage Team →</button></div></div>
   <div class="card market"><div class="cardh"><div><h2>Player Selection</h2><div id="marketCount" class="muted"></div></div><div class="price">Launch price + gradual market pricing</div></div><div class="toolbar"><input id="search" class="ctrl" placeholder="Search player..."><select id="clubFilter" class="ctrl"><option value="">All clubs</option></select><select id="posFilter" class="ctrl"><option value="">All positions</option><option>GK</option><option>DEF</option><option>MID</option><option>FWD</option></select><select id="sortFilter" class="ctrl"><option value="recommended">Recommended</option><option value="price_desc">Price ↓</option><option value="price_asc">Price ↑</option><option value="points_desc">Points ↓</option><o

=== HTML $('leagueImportPreference') ===
index=768138

<div class="highlightValue">${
value}
</div></div>`;
$('statsHighlights').innerHTML=high('☆',p0,'Leads Fantasy Points',`${
Number(p0?.fantasy_points||0)}
 pts`)+high('⚽',g0,'Top Goal Scorer',`${
Number(g0?.goals||0)}
 goals`)+high('⌁',a0,'Most Assists',`${
Number(a0?.assists||0)}
 assists`)+high('£',v0,'Most Expensive Player',`£${
Number(v0?.price||0).toFixed(1)}
m`);

}

function renderSidebar(){
const live=state.teamConfirmed?liveManagerGameweek():null;
$('sideGW').textContent=live?`GW ${
live.gw}
 Live`:`Next GW ${
state.teamConfirmed?state.currentGameweek:META.current_gameweek}
`;
$('sideCompleted').textContent=`GW ${
META.completed_gameweek||0}
`;
$('sideTeam').textContent=state.teamConfirmed?state.teamName:'Not entered';
$('sideFT').textContent=!state.teamConfirmed||!rulesActive()?'Unlimited':`${
state.freeTransfers}
/5 FT`;
$('sidePts').textContent=state.teamConfirmed?managerDisplayTotal():(state.totalPoints||0)}


function fitActivePage(){

 const main=document.querySelector('.main'),top=document.querySelector('.top'),page=document.querySelector('.page.active');

 if(!main||!top||!page)return;

 page.style.zoom='1';
page.style.width='100%';

 const mt=parseFloat(getComputedStyle(page).marginTop||0)||0;

 const availH=Math.max(120,main.clientHeight-top.offsetHeight-mt-4);

 const availW=Math.max(240,main.clientWidth);

 let naturalH=Math.max(1,page.scrollHeight),naturalW=Math.max(1,page.scrollWidth);

 let scale=Math.min(1,availH/naturalH,availW/naturalW);

 if(scale<.999){

   page.style.zoom=String(scale);

   page.style.width=(100/scale)+'%';

   // Re-measure once after widening because responsive grids may become shorter.
   requestAnimationFrame(()=>{

     page.style.zoom='1';

     const h2=Math.max(1,page.scrollHeight),w2=Math.max(1,page.scrollWidth);

     let s2=Math.min(1,availH/h2,availW/w2);

     page.style.zoom=String(s2);

     page.style.width=(100/s2)+'%';

   }
);

 }

}


function renderAll(){
renderSidebar();
renderTransferPitch();
renderTransferSummary();
renderMarket();
renderTeam();
renderFixtures();
renderStar();
renderTable();
renderLeagues();
renderStats();
renderNews();
save();
requestAnimationFrame(fitActivePage)}

refreshCompetitionUI();
refreshClubFilters()

function refreshCompetitionUI(){
const max=seasonGWMax(),label=leagueLabel();
if($('competitionTopLabel'))$('competitionTopLabel').textContent=`FM Fantasy ${
label}
`;
if($('leagueTableHeading'))$('leagueTableHeading').textContent=label;
if($('fixtureCalendarLabel'))$('fixtureCalendarLabel').textContent=`${
max}
 calendar Gameweeks`;
if($('leagueImportPreference'))$('leagueImportPreference').dataset.loadedCompetition=META.competition||'';
const sel=$('fixtureGW');
if(sel){
const keep=Math.max(1,Math.min(max,Number(sel.value||state.currentGameweek||1)));
sel.innerHTML=Array.from({
length:max}
,(_,i)=>`<option value="${
i+1}
">GW ${
i+1}
</option>`).join('');
sel.value=String(keep)}
}

function refreshClubFilters(){

 for(const id of ['clubFilter','fixtureClub']){
const el=$(id);
if(!el)continue;
const keep=el.value;
el.innerHTML='<option value="">All clubs</option>';
for(const c of [...new Set(PLAYERS.filter(p=>p.visible&&p.available!==false).map(p=>p.club))].sort())el.insertAdjacentHTML('beforeend',`<option>${
esc(c)}
</option>`);
if([...el.options].some(o=>o.value===keep))el.value=keep}

}


function fmApplyPostPayloadPricingCorrections(payload){

 payload=payload||{
}
;
payload.meta=payload.meta||{
}
;
let changed=0;

 for(const p of payload.players||[]){

  const c=p.price_context||(p.price_context={
}
);
const pos=String(p.pos||'');
if(!['GK','DEF','MID','FWD'].includes(pos))continue;

  let price=Number(p.price||0),q=Number(c.quality||0),d=Number(c.depth_score||0),ca=Number(p.ca||0),mins=Number(p.minutes||0),apps=Number(p.apps||0);

  const st=[p.injury_status,p.suspension_status,p.status].filter(Boolean).join(' ').toLowerCase();

  const unavailable=!!(p.injured||p.suspended||st.includes('injur')||st.includes('suspend')||p.return_date||p.injury_return_date||p.injury_evidence);

  if(mins<=0&&unavailable&&(q>=0.55||d>=0.55||ca>=130)){

   let floor={
GK:4.5,DEF:5.0,MID:5.5,FWD:6.0}
[pos];
if(q>=0.80&&d>=0.70)floor+=0.5;

   if(price<floor){
price=floor;
changed++}

   c.usage_role='established_absence';
c.availability_signal='Established senior role · currently unavailable';
c.pricing_guardrail='v67_postpayload_established_absence';

  }

  const avail=Number(c.available_matches||0),late=!!c.late_arrival;

  if(late&&avail<=1&&apps<=1&&q>=0.60&&d>=0.45){

   let proj=pos==='GK'?4.0+0.60*q+0.40*d:pos==='DEF'?4.5+0.80*q+0.50*d:pos==='MID'?5.0+1.30*q+0.60*d:5.5+1.40*q+0.70*d;

   proj=Math.round(proj*2)/2;

   if(price<proj){
price=proj;
changed++}

   c.usage_role='new_arrival_projected';
c.availability_signal='Recent arrival · projected from quality and squad role';
c.pricing_guardrail='v67_postpayload_new_arrival_projection';
c.projected_arrival_price=proj;

  }

  if(c.pricing_guardrail){
p.price=price;
p.model_price=price;
c.price=price;
}

 }

 payload.meta.postpayload_pricing_corrections=changed;
payload.meta.price_merge_policy='v67 post-payload corrective pricing before launch freeze/dynamic merge';
return payload;

}

function fmHistoryKey(h){
return `${
h?.date||''}
|${
h?.home||''}
|${
h?.away||''}
|${
h?.opponent||''}
|${
h?.venue||''}
`}

function fmPriceTrackerSeed(p,gw){
return{
samples:[],consecutiveStarts:0,consecutiveMisses:0,absenceCheckpoint:0,startCheckpoint:0,lastHistoryCount:(p.history||[]).length,baselineNailedness:Number(p.price_context?.nailedness||0),baselineRole:String(p.price_context?.availability_signal||''),baselineGameweek:Number(gw||0),processedHistoryKeys:[...new Set((p.history||[]).map(fmHistoryKey))],importsObserved:0}
}

function fmInitialiseLaunchPrices(payload){
const gw=Number(payload?.meta?.latest_gameweek_with_result||0);
for(const p of payload.players||[]){
p.model_price=Number(p.price||0);
p.launch_price=Number(p.price||0);
p.dynamic_price=Number(p.price||0);
p.price=Number(p.price||0);
p.price_baseline_gw=gw;
p.price_change_history=[];
p.price_tracker=fmPriceTrackerSeed(p,gw);
}
payload.meta=payload.meta||{
}
;
payload.meta.price_baseline_gw=gw;
payload.meta.price_policy='launch price frozen at database import;
 only post-import sustained form can move price';
}

function fmPriceSample(h){
const mins=Number(h?.minutes||0),pts=Number(h?.fpl_points||0),ga=Number(h?.goals||0)+Number(h?.assists||0),rating=Number(h?.rating||0);
if(mins<=0)return null;
if(ga>0||pts>=7||rating>=7.4)return 1;
if(mins>=60&&ga===0&&pts<=2&&(rating===0||rating<=6.6))return -1;
return 0}

function fmApplyDynamicPricing(oldP,payload,changes){
const oldMap=new Map((oldP.players||[]).map(p=>[String(p.pid),p])),inc=[],dec=[];
for(const p of payload.players||[]){
const o=oldMap.get(String(p.pid));
p.model_price=Number(p.price||0);
if(!o){
p.launch_price=Number(p.price||0);
p.dynamic_price=Number(p.price||0);
p.price_baseline_gw=Number(payload.meta?.latest_gameweek_with_result||0);
p.price_change_history=[];
p.price_tracker=fmPriceTrackerSeed(p,p.price_baseline_gw);
continue}

 const oldPrice=Number((o.price??o.dynamic_price??o.launch_price??p.price)||0);

 // v6

=== HTML FM_PENDING_SEASON_LEAGUE ===
index=792393

}
,DEFAULT,{
leagues,completedGameweek:META.completed_gameweek,currentGameweek:META.current_gameweek,bank:100,lockedBank:100,news:null}
);

   state.chips=JSON.parse(JSON.stringify(DEFAULT.chips));
state.news=fmBuildInitialNews(payload);

 }
else if(!state.teamConfirmed){
state.completedGameweek=META.completed_gameweek;
state.currentGameweek=META.current_gameweek}

 else syncManagerProgressFromHistory();

 if(mode==='load'&&!state.news){
state.news=fmBuildInitialNews(payload)}

 fixtureViewGW=(state.teamConfirmed?state.currentGameweek:META.current_gameweek)||1;
starViewGW=Math.max(1,META.completed_gameweek||1);
teamViewGW=state.teamConfirmed?state.currentGameweek:(META.current_gameweek||1);

 fmRecalculateBank();
refreshCompetitionUI();
refreshClubFilters();
save();
renderAll();

}

function syncMessage(text,kind=''){
const el=$('syncStatus');
el.textContent=text;
el.className='syncStatus '+kind}

function syncProgress(pct,label='',bad=false){
const n=Math.max(0,Math.min(100,Math.round(Number(pct)||0))),wrap=$('syncProgress');
wrap.classList.add('show');
wrap.classList.toggle('bad',!!bad);
$('syncProgressFill').style.width=n+'%';
$('syncProgressPct').textContent=n+'%';
if(label)$('syncProgressLabel').textContent=label}

function syncBusy(on){
$('seasonImportBtn').disabled=on;
$('updateImportBtn').disabled=on;
$('clearDatabaseBtn').disabled=on}

async function clearFMDatabase(){

 if(!confirm('Clear the imported FM database and reset this fantasy save, squad, points and leagues on this browser?'))return;

 syncBusy(true);

 try{
await fmStoredClear();
PLAYERS.splice(0);
MATCHES.splice(0);
SEASON_FIXTURES.splice(0);
CHAMP_TABLE.splice(0);
for(const k of Object.keys(STAR_TEAMS))delete STAR_TEAMS[k];
for(const k of Object.keys(META))delete META[k];
Object.assign(META,{
named_players:0,hidden_unresolved:0,completed_gameweek:0,current_gameweek:1,total_matches_extracted:0,fully_completed_gameweeks:[]}
);
state=fmDeepClone(DEFAULT);
state.chips=fmDeepClone(DEFAULT.chips);
localStorage.removeItem('fmfantasy_final_v5');
localStorage.removeItem(FM_DEBUG_KEY);
FM_DEBUG={
version:'v27-postpayload-pricing',startedAt:null,mode:null,file:null,events:[],lastError:null,lastMeta:null}
;
fixtureViewGW=1;
starViewGW=1;
teamViewGW=1;
refreshCompetitionUI();
refreshClubFilters();
save();
renderAll();
$('syncProgress').classList.remove('show','bad');
syncMessage('Database cleared — ready for a fresh Season / Database Import.','ok')}

 catch(e){
syncMessage('Could not clear database: '+String(e?.message||e),'bad')}

 finally{
syncBusy(false)}

}

let FM_PENDING_SEASON_LEAGUE=null;

const FM_LEAGUE_PREF_KEY='fmFantasySeasonLeaguePreferenceV72';

function fmNormaliseLeaguePreference(v){
return v==='Premier League'||v==='EFL Championship'?v:null}

function fmRememberLeaguePreference(v){
const p=fmNormaliseLeaguePreference(v);
try{
if(p)sessionStorage.setItem(FM_LEAGUE_PREF_KEY,p);
else sessionStorage.removeItem(FM_LEAGUE_PREF_KEY)}
catch(_){
}
return p}

function fmCurrentLeaguePreference(){
const ui=fmNormaliseLeaguePreference($('leagueImportPreference')?.value);
if(ui)return ui;
const pending=fmNormaliseLeaguePreference(FM_PENDING_SEASON_LEAGUE);
if(pending)return pending;
try{
return fmNormaliseLeaguePreference(sessionStorage.getItem(FM_LEAGUE_PREF_KEY))}
catch(_){
return null}
}

try{
const sel=$('leagueImportPreference'),saved=fmNormaliseLeaguePreference(sessionStorage.getItem(FM_LEAGUE_PREF_KEY));
if(sel&&saved&&!sel.value)sel.value=saved;
if(sel)sel.onchange=()=>{
FM_PENDING_SEASON_LEAGUE=fmRememberLeaguePreference(sel.value)}
}
catch(_){
}


async function sendFMImport(file,mode,autoRetry=false,explicitPreference=undefined){

 if(!file)return;

 if(mode==='season'&&state.teamConfirmed&&!confirm('Season / Database Import rebuilds the FM world and resets your fantasy squad. Continue?'))return;

 if(!autoRetry)fmDebugReset(file,mode);
syncBusy(true);
syncProgress(1,mode==='season'?'Starting season import…':'Starting Gameweek update…');
syncMessage(mode==='season'?'Reading FM season/database…':'Checking FM gameweek update…');

 try{

  const old=await fmStoredGet();
if(mode==='update'&&!old)throw new Error('Run Season / Database Import first.');

  const preferredLeague=mode==='update'?(old?.meta?.competition||null):(explicitPreference!==undefined?fmNormaliseLeaguePreference(explicitPreference):fmCurrentLeaguePreference());
fmDebugAdd('info',`League request: ${
preferredLeague||'AUTO'}
${
mode==='season'?' (captured before file selection)':''}
`);
const result=await FM_RUNTIME.importFile(file,(t,pct)=>{
syncMessage(t);
syncProgress(pct,t);
fmDebugAdd('progress',t,{
percent:pct}
)}
,mode==='update'?old?.meta?.fingerprint:null,preferredLeague);

  if(result.duplicate&&String(old?.meta?.price_merge_policy||'').includes('v67')){
fmDebugAdd('result','Duplicate save detected — no database changes applied.');
applyImportedPayload(old,'update');
const processed=fmProcessCompletedGameweeks(),live=liveManagerGameweek();
syncProgress(100,'Already up to date');
syncMessage(processed.length?`Same save · repaired ${
processed.length}
 completed Gameweek${
processed.length===1?'':'s'}
 into your team total.`:live?`Same save · GW ${
live.gw}
 remains ongoing at ${
live.gross}
 live team pts.`:'Same save already imported — 0 database changes, nothing double-counted.','ok');
return}

  if(result.duplicate){
result.duplicate=false;
fmDebugAdd('info','Same save accepted once to apply v6.7 pricing correction.');
}

  let payload=result.payload,changes=null;

  fmApplyPostPayloadPricingCorrections(payload);

  if(mode==='update'){

   changes=fmDiff(old,payload);
const newIds=new Set((payload.players||[]).map(p=>String(p.pid))),retained=(old.players||[]).filter(p=>!newIds.has(String(p.pid))).map(p=>Object.assign({
}
,p,{
available:false,departed:true,visible:true}
));
if(retained.length){
payload.players.push(...retained);
payload.meta.historical_players_retained=retained.length}

   fmMergeHistoricalFixtures(old,payload);
fmMergeHistoricalMatches(old,payload);
fmRebuildPlayerAggregates(payload);
const priceMoves=fmApplyDynamicPricing(old,payload,changes);
state.news=fmBuildImportNews(changes,priceMoves,payload,old);

  }
else{
fmInitialiseLaunchPrices(payload)}

  fmDeduplicateRichMatches(payload);
fmRebuildPlayerAggregates(payload);
payload.star_teams=fmBuildStars(payload.players||[],Number(payload.meta?.completed_gameweek||0));
FM_DEBUG.lastMeta=payload.meta||null;
fmDebugAdd('result','Payload built successfully.',payload.meta||{
}
);
try{
const lm=JSON.parse(localStorage.getItem('fmfantasy_league_by_fingerprint_v1')||'{
}
');
if(payload.meta?.fingerprint&&payload.meta?.competition){
lm[payload.meta.fingerprint]=payload.meta.competition;
localStorage.setItem('fmfantasy_league_by_fingerprint_v1',JSON.stringify(lm))}
}
catch(_e){
}
;
syncProgress(99,'Applying imported database…');
await fmStoredSet(payload);
applyImportedPayload(payload,mode);
const processed=mode==='update'?fmProcessCompletedGameweeks():[];
const live=mode==='update'?liveManagerGameweek():null;

  syncProgress(100,'Import complete');

  if(mode==='season'){

    const played=Number(payload.meta.played_results||0),rich=Number(payl

=== HTML msg.includes('Multiple supported English league seasons') ===
index=-1

=== HTML seasonImportBtn').addEventListener ===
index=-1

=== HTML updateImportBtn').addEventListener ===
index=-1

=== HTML sendFMImport( ===
index=793394

yncProgress');
wrap.classList.add('show');
wrap.classList.toggle('bad',!!bad);
$('syncProgressFill').style.width=n+'%';
$('syncProgressPct').textContent=n+'%';
if(label)$('syncProgressLabel').textContent=label}

function syncBusy(on){
$('seasonImportBtn').disabled=on;
$('updateImportBtn').disabled=on;
$('clearDatabaseBtn').disabled=on}

async function clearFMDatabase(){

 if(!confirm('Clear the imported FM database and reset this fantasy save, squad, points and leagues on this browser?'))return;

 syncBusy(true);

 try{
await fmStoredClear();
PLAYERS.splice(0);
MATCHES.splice(0);
SEASON_FIXTURES.splice(0);
CHAMP_TABLE.splice(0);
for(const k of Object.keys(STAR_TEAMS))delete STAR_TEAMS[k];
for(const k of Object.keys(META))delete META[k];
Object.assign(META,{
named_players:0,hidden_unresolved:0,completed_gameweek:0,current_gameweek:1,total_matches_extracted:0,fully_completed_gameweeks:[]}
);
state=fmDeepClone(DEFAULT);
state.chips=fmDeepClone(DEFAULT.chips);
localStorage.removeItem('fmfantasy_final_v5');
localStorage.removeItem(FM_DEBUG_KEY);
FM_DEBUG={
version:'v27-postpayload-pricing',startedAt:null,mode:null,file:null,events:[],lastError:null,lastMeta:null}
;
fixtureViewGW=1;
starViewGW=1;
teamViewGW=1;
refreshCompetitionUI();
refreshClubFilters();
save();
renderAll();
$('syncProgress').classList.remove('show','bad');
syncMessage('Database cleared — ready for a fresh Season / Database Import.','ok')}

 catch(e){
syncMessage('Could not clear database: '+String(e?.message||e),'bad')}

 finally{
syncBusy(false)}

}

let FM_PENDING_SEASON_LEAGUE=null;

const FM_LEAGUE_PREF_KEY='fmFantasySeasonLeaguePreferenceV72';

function fmNormaliseLeaguePreference(v){
return v==='Premier League'||v==='EFL Championship'?v:null}

function fmRememberLeaguePreference(v){
const p=fmNormaliseLeaguePreference(v);
try{
if(p)sessionStorage.setItem(FM_LEAGUE_PREF_KEY,p);
else sessionStorage.removeItem(FM_LEAGUE_PREF_KEY)}
catch(_){
}
return p}

function fmCurrentLeaguePreference(){
const ui=fmNormaliseLeaguePreference($('leagueImportPreference')?.value);
if(ui)return ui;
const pending=fmNormaliseLeaguePreference(FM_PENDING_SEASON_LEAGUE);
if(pending)return pending;
try{
return fmNormaliseLeaguePreference(sessionStorage.getItem(FM_LEAGUE_PREF_KEY))}
catch(_){
return null}
}

try{
const sel=$('leagueImportPreference'),saved=fmNormaliseLeaguePreference(sessionStorage.getItem(FM_LEAGUE_PREF_KEY));
if(sel&&saved&&!sel.value)sel.value=saved;
if(sel)sel.onchange=()=>{
FM_PENDING_SEASON_LEAGUE=fmRememberLeaguePreference(sel.value)}
}
catch(_){
}


async function sendFMImport(file,mode,autoRetry=false,explicitPreference=undefined){

 if(!file)return;

 if(mode==='season'&&state.teamConfirmed&&!confirm('Season / Database Import rebuilds the FM world and resets your fantasy squad. Continue?'))return;

 if(!autoRetry)fmDebugReset(file,mode);
syncBusy(true);
syncProgress(1,mode==='season'?'Starting season import…':'Starting Gameweek update…');
syncMessage(mode==='season'?'Reading FM season/database…':'Checking FM gameweek update…');

 try{

  const old=await fmStoredGet();
if(mode==='update'&&!old)throw new Error('Run Season / Database Import first.');

  const preferredLeague=mode==='update'?(old?.meta?.competition||null):(explicitPreference!==undefined?fmNormaliseLeaguePreference(explicitPreference):fmCurrentLeaguePreference());
fmDebugAdd('info',`League request: ${
preferredLeague||'AUTO'}
${
mode==='season'?' (captured before file selection)':''}
`);
const result=await FM_RUNTIME.importFile(file,(t,pct)=>{
syncMessage(t);
syncProgress(pct,t);
fmDebugAdd('progress',t,{
percent:pct}
)}
,mode==='update'?old?.meta?.fingerprint:null,preferredLeague);

  if(result.duplicate&&String(old?.meta?.price_merge_policy||'').includes('v67')){
fmDebugAdd('result','Duplicate save detected — no database changes applied.');
applyImportedPayload(old,'update');
const processed=fmProcessCompletedGameweeks(),live=liveManagerGameweek();
syncProgress(100,'Already up to date');
syncMessage(processed.length?`Same save · repaired ${
processed.length}
 completed Gameweek${
processed.length===1?'':'s'}
 into your team total.`:live?`Same save · GW ${
live.gw}
 remains ongoing at ${
live.gross}
 live team pts.`:'Same save already imported — 0 database changes, nothing double-counted.','ok');
return}

  if(result.duplicate){
result.duplicate=false;
fmDebugAdd('info','Same save accepted once to apply v6.7 pricing correction.');
}

  let payload=result.payload,changes=null;

  fmApplyPostPayloadPricingCorrections(payload);

  if(mode==='update'){

   changes=fmDiff(old,payload);
const newIds=new Set((payload.players||[]).map(p=>String(p.pid))),retained=(old.players||[]).filter(p=>!newIds.has(String(p.pid))).map(p=>Object.assign({
}
,p,{
available:false,departed:true,visible:true}
));
if(retained.length){
payload.players.push(...retained);
payload.meta.historical_players_retained=retained.length}

   fmMergeHistoricalFixtures(old,payload);
fmMergeHistoricalMatches(old,payload);
fmRebuildPlayerAggregates(payload);
const priceMoves=fmApplyDynamicPricing(old,payload,changes);
state.news=fmBuildImportNews(changes,priceMoves,payload,old);

  }
else{
fmInitialiseLaunchPrices(payload)}

  fmDeduplicateRichMatches(payload);
fmRebuildPlayerAggregates(payload);
payload.star_teams=fmBuildStars(payload.players||[],Number(payload.meta?.completed_gameweek||0));
FM_DEBUG.lastMeta=payload.meta||null;
fmDebugAdd('result','Payload built successfully.',payload.meta||{
}
);
try{
const lm=JSON.parse(localStorage.getItem('fmfantasy_league_by_fingerprint_v1')||'{
}
');
if(payload.meta?.fingerprint&&payload.meta?.competition){
lm[payload.meta.fingerprint]=payload.meta.competition;
localStorage.setItem('fmfantasy_league_by_fingerprint_v1',JSON.stringify(lm))}
}
catch(_e){
}
;
syncProgress(99,'Applying imported database…');
await fmStoredSet(payload);
applyImportedPayload(payload,mode);
const processed=mode==='update'?fmProcessCompletedGameweeks():[];
const live=mode==='update'?liveManagerGameweek():null;

  syncProgress(100,'Import complete');

  if(mode==='season'){

    const played=Number(payload.meta.played_results||0),rich=Number(payload.meta.rich_matches||0),missing=Math.max(0,played-rich);

    if(played>0&&missing>0)syncMessage(`${
payload.meta.players}
 players · ${
payload.meta.fixtures}
 fixtures · ${
played}
 results loaded · player detail ${
rich}
/${
played}
 matches (${
missing}
 missing). Database loaded with partial historical player coverage — no automatic re-scan was run.`,'warn');

    else syncMessage(`${
payload.meta.players}
 players · ${
payload.meta.fixtures}
 fixtures · ${
played}
 results loaded · player detail ${
rich}
/${
played}
 matches. Historical coverage complete. Calendar Gameweeks ready.`,'ok');

  }

  else{
const pts=processed.reduce((s,x)=>s+Number(x.net||0),0);
syncMessage(`${
changes.new_results.length}
 new results · ${
changes.new_rich_matches}
 new rich matches · ${
processed.length}
 Gameweek${
processed.length===1?'':'s'}
 finalised${
processed.length?` · ${
pts}
 net pts`:''}
${
live?` · GW ${
live.gw}
 ongoing: ${
live.gross}
 live team pts`:''}
.`,'ok')}

 }
catch(e){

  const msg=String(e?.message||e||'');
console.error(

=== HTML function refreshCompetitionUI ===
index=767808

ht('Goals Per Game','Top 5 players',gg)+insight('Assists Per Game','Top 5 players',ag)+insight('Most Clean Sheets','Top 5 goalkeepers',cs,x=>Math.round(x));

 const high=(icon,p,label,value)=>`<div class="highlightRow"><div class="highlightIcon">${
icon}
</div><div><b>${
p?esc(playerName(p)):'No data'}
</b><span>${
label}
</span></div><div class="highlightValue">${
value}
</div></div>`;
$('statsHighlights').innerHTML=high('☆',p0,'Leads Fantasy Points',`${
Number(p0?.fantasy_points||0)}
 pts`)+high('⚽',g0,'Top Goal Scorer',`${
Number(g0?.goals||0)}
 goals`)+high('⌁',a0,'Most Assists',`${
Number(a0?.assists||0)}
 assists`)+high('£',v0,'Most Expensive Player',`£${
Number(v0?.price||0).toFixed(1)}
m`);

}

function renderSidebar(){
const live=state.teamConfirmed?liveManagerGameweek():null;
$('sideGW').textContent=live?`GW ${
live.gw}
 Live`:`Next GW ${
state.teamConfirmed?state.currentGameweek:META.current_gameweek}
`;
$('sideCompleted').textContent=`GW ${
META.completed_gameweek||0}
`;
$('sideTeam').textContent=state.teamConfirmed?state.teamName:'Not entered';
$('sideFT').textContent=!state.teamConfirmed||!rulesActive()?'Unlimited':`${
state.freeTransfers}
/5 FT`;
$('sidePts').textContent=state.teamConfirmed?managerDisplayTotal():(state.totalPoints||0)}


function fitActivePage(){

 const main=document.querySelector('.main'),top=document.querySelector('.top'),page=document.querySelector('.page.active');

 if(!main||!top||!page)return;

 page.style.zoom='1';
page.style.width='100%';

 const mt=parseFloat(getComputedStyle(page).marginTop||0)||0;

 const availH=Math.max(120,main.clientHeight-top.offsetHeight-mt-4);

 const availW=Math.max(240,main.clientWidth);

 let naturalH=Math.max(1,page.scrollHeight),naturalW=Math.max(1,page.scrollWidth);

 let scale=Math.min(1,availH/naturalH,availW/naturalW);

 if(scale<.999){

   page.style.zoom=String(scale);

   page.style.width=(100/scale)+'%';

   // Re-measure once after widening because responsive grids may become shorter.
   requestAnimationFrame(()=>{

     page.style.zoom='1';

     const h2=Math.max(1,page.scrollHeight),w2=Math.max(1,page.scrollWidth);

     let s2=Math.min(1,availH/h2,availW/w2);

     page.style.zoom=String(s2);

     page.style.width=(100/s2)+'%';

   }
);

 }

}


function renderAll(){
renderSidebar();
renderTransferPitch();
renderTransferSummary();
renderMarket();
renderTeam();
renderFixtures();
renderStar();
renderTable();
renderLeagues();
renderStats();
renderNews();
save();
requestAnimationFrame(fitActivePage)}

refreshCompetitionUI();
refreshClubFilters()

function refreshCompetitionUI(){
const max=seasonGWMax(),label=leagueLabel();
if($('competitionTopLabel'))$('competitionTopLabel').textContent=`FM Fantasy ${
label}
`;
if($('leagueTableHeading'))$('leagueTableHeading').textContent=label;
if($('fixtureCalendarLabel'))$('fixtureCalendarLabel').textContent=`${
max}
 calendar Gameweeks`;
if($('leagueImportPreference'))$('leagueImportPreference').dataset.loadedCompetition=META.competition||'';
const sel=$('fixtureGW');
if(sel){
const keep=Math.max(1,Math.min(max,Number(sel.value||state.currentGameweek||1)));
sel.innerHTML=Array.from({
length:max}
,(_,i)=>`<option value="${
i+1}
">GW ${
i+1}
</option>`).join('');
sel.value=String(keep)}
}

function refreshClubFilters(){

 for(const id of ['clubFilter','fixtureClub']){
const el=$(id);
if(!el)continue;
const keep=el.value;
el.innerHTML='<option value="">All clubs</option>';
for(const c of [...new Set(PLAYERS.filter(p=>p.visible&&p.available!==false).map(p=>p.club))].sort())el.insertAdjacentHTML('beforeend',`<option>${
esc(c)}
</option>`);
if([...el.options].some(o=>o.value===keep))el.value=keep}

}


function fmApplyPostPayloadPricingCorrections(payload){

 payload=payload||{
}
;
payload.meta=payload.meta||{
}
;
let changed=0;

 for(const p of payload.players||[]){

  const c=p.price_context||(p.price_context={
}
);
const pos=String(p.pos||'');
if(!['GK','DEF','MID','FWD'].includes(pos))continue;

  let price=Number(p.price||0),q=Number(c.quality||0),d=Number(c.depth_score||0),ca=Number(p.ca||0),mins=Number(p.minutes||0),apps=Number(p.apps||0);

  const st=[p.injury_status,p.suspension_status,p.status].filter(Boolean).join(' ').toLowerCase();

  const unavailable=!!(p.injured||p.suspended||st.includes('injur')||st.includes('suspend')||p.return_date||p.injury_return_date||p.injury_evidence);

  if(mins<=0&&unavailable&&(q>=0.55||d>=0.55||ca>=130)){

   let floor={
GK:4.5,DEF:5.0,MID:5.5,FWD:6.0}
[pos];
if(q>=0.80&&d>=0.70)floor+=0.5;

   if(price<floor){
price=floor;
changed++}

   c.usage_role='established_absence';
c.availability_signal='Established senior role · currently unavailable';
c.pricing_guardrail='v67_postpayload_established_absence';

  }

  const avail=Number(c.available_matches||0),late=!!c.late_arrival;

  if(late&&avail<=1&&apps<=1&&q>=0.60&&d>=0.45){

   let proj=pos==='GK'?4.0+0.60*q+0.40*d:pos==='DEF'?4.5+0.80*q+0.50*d:pos==='MID'?5.0+1.30*q+0.60*d:5.5+1.40*q+0.70*d;

   proj=Math.round(proj*2)/2;

   if(price<proj){
price=proj;
changed++}

   c.usage_role='new_arrival_projected';
c.availability_signal='Recent arrival · projected from quality and squad role';
c.pricing_guardrail='v67_postpayload_new_arrival_projection';
c.projected_arrival_price=proj;

  }

  if(c.pricing_guardrail){
p.price=price;
p.model_price=price;
c.price=price;
}

 }

 payload.meta.postpayload_pricing_corrections=changed;
payload.meta.price_merge_policy='v67 post-payload corrective pricing before launch freeze/dynamic merge';
return payload;

}

function fmHistoryKey(h){
return `${
h?.date||''}
|${
h?.home||''}
|${
h?.away||''}
|${
h?.opponent||''}
|${
h?.venue||''}
`}

function fmPriceTrackerSeed(p,gw){
return{
samples:[],consecutiveStarts:0,consecutiveMisses:0,absenceCheckpoint:0,startCheckpoint:0,lastHistoryCount:(p.history||[]).length,baselineNailedness:Number(p.price_context?.nailedness||0),baselineRole:String(p.price_context?.availability_signal||''),baselineGameweek:Number(gw||0),processedHistoryKeys:[...new Set((p.history||[]).map(fmHistoryKey))],importsObserved:0}
}

function fmInitialiseLaunchPrices(payload){
const gw=Number(payload?.meta?.latest_gameweek_with_result||0);
for(const p of payload.players||[]){
p.model_price=Number(p.price||0);
p.launch_price=Number(p.price||0);
p.dynamic_price=Number(p.price||0);
p.price=Number(p.price||0);
p.price_baseline_gw=gw;
p.price_change_history=[];
p.price_tracker=fmPriceTrackerSeed(p,gw);
}
payload.meta=payload.meta||{
}
;
payload.meta.price_baseline_gw=gw;
payload.meta.price_policy='launch price frozen at database import;
 only post-import sustained form can move price';
}

function fmPriceSample(h){
const mins=Number(h?.minutes||0),pts=Number(h?.fpl_points||0),ga=Number(h?.goals||0)+Number(h?.assists||0),rating=Number(h?.rating||0);
if(mins<=0)return null;
if(ga>0||pts>=7||rating>=7.4)return 1;
if(mins>=60&&ga===0&&pts<=2&&(rating===0||rating<=6.6))return -1;
return 0}

function fmApplyDynamicPricing(oldP,payload,changes){
const oldMap=new Map((oldP.players||[]).map(p=>[String(p.pid),p])),inc=[],dec=[];
for(const p of payload.players||[]){
const o=oldMap.get(String(p.pid));
p.model_price=Number(p.price||0)

=== PY def select_championship_fixtures ===
index=7396

s can move
    between databases and future seasons.
    """
    raw=[]
    for p in range(11,len(fix)-40):
        if fix[p+4:p+7] != b'\xff\x00\x00' or fix[p+11:p+13] != b'\xff\x00':
            continue
        comp=u32(fix,p-11);
 home,away=u32(fix,p),u32(fix,p+7)
        stamp,year=u16(fix,p+13),u16(fix,p+15);
 doy=stamp&0x1FF;
 gw0=fix[p+38]
        if not (1<=doy<=366 and 2024<=year<=2045 and 0<=gw0<=60):continue
        date=fm_date(stamp,year)
        raw.append({
'fixture_offset':p,'competition_id':comp,'home_tid':home,'away_tid':away,
                    'date':date,'date_stamp':stamp,'year':year,'raw_gameweek':gw0+1,'gameweek':gw0+1,
                    'season_start':_season_start_for_date(date),'status':'future','home_score':None,'away_score':None,
                    'source':'fm_fix_man'}
)
    grouped=collections.defaultdict(list)
    for r in raw: grouped[(r['competition_id'],r['season_start'])].append(r)
    groups=[]
    shapes={
(v['fixtures'],v['teams'],v['rounds'],v['per_round']):name for name,v in SUPPORTED_LEAGUES.items()}

    for (comp,season),rows in grouped.items():
        ded=[];
seen=set()
        for r in sorted(rows,key=lambda x:x['fixture_offset']):
            k=(r['home_tid'],r['away_tid'],r['date'],r['raw_gameweek'])
            if k in seen:continue
            seen.add(k);
ded.append(r)
        teams={
x for r in ded for x in (r['home_tid'],r['away_tid'])}

        gws=collections.Counter(r['raw_gameweek'] for r in ded)
        league_name=None
        for (fixture_count,team_count,round_count,per_round),name in shapes.items():
            if (len(ded)==fixture_count and len(teams)==team_count and
                set(gws)==set(range(1,round_count+1)) and all(v==per_round for v in gws.values())):
                league_name=name;
break
        groups.append({
'competition_id':comp,'season_start':season,'rows':ded,'teams':teams,'gws':gws,
                       'exact':league_name is not None,'league_shape':league_name}
)
    return groups


def select_championship_fixtures(fix:bytes,clubs:dict[int,Club],expected_names:set[str]|None=None,
                                 expected_league:str|None=None)->tuple[list[dict[str,Any]],dict[str,Any]]:
    """Select the current supported domestic league without trusting a fixed FM competition id.

    Manual league selection is a HARD constraint.  Auto detection never allows old retained
    match-detail from an earlier season/division to drag the save back into that old league.
    It first restricts candidates to the newest season present in the fixture calendar.
    """
    groups=scan_fixture_groups(fix)
    exact_all=[g for g in groups if g['exact']]
    if not exact_all:
        near=sorted(groups,key=lambda g:(abs(len(g['teams'])-24),-g['season_start']))[:10]
        summary=';
 '.join(f"comp={
g['competition_id']}
 season={
g['season_start']}
 fixtures={
len(g['rows'])}
 teams={
len(g['teams'])}
 gws={
len(g['gws'])}
 shape={
g.get('league_shape')}
" for g in near)
        raise RuntimeError('No supported full league season could be identified. Candidates: '+summary)

    requested=expected_league if expected_league in SUPPORTED_LEAGUES else None
    if requested:
        exact=[g for g in exact_all if g.get('league_shape')==requested]
        if not exact:
            raise RuntimeError(f'Manual league selection was {
requested}
, but no complete {
requested}
 season exists in this save. The selection will not be overridden.')
        newest=max(g['season_start'] for g in exact)
        exact=[g for g in exact if g['season_start']==newest]
        selection_mode='user_preference_locked'
    else:
        newest=max(g['season_start'] for g in exact_all)
        exact=[g for g in exact_all if g['season_start']==newest]
        shapes={
g.get('league_shape') for g in exact}

        if len(shapes)>1:
            summary=', '.join(sorted(str(x) for x in shapes))
            raise RuntimeError(f'Current season {
newest}
/{
str(newest+1)[-2:]}
 contains both supported English leagues ({
summary}
). Historical retained-match identity is deliberately ignored because a promoted/relegated human club may have old division data. Choose the current league once;
 the choice will be locked for this import.')
        selection_mode='auto_latest_season'

    scored=[]
    candidate_debug=[]
    for g in exact:
        try:
            sh=derive_fixture_to_club_shift(g['teams'],clubs,expected_names if requested else None)
            mapped={
normalize_club_name(clubs[tid-sh].short or clubs[tid-sh].name) for tid in g['teams'] if tid-sh in clubs}

            overlap=len(mapped & expected_names) if (requested and expected_names) else 0
            legacy_hint=1 if (g.get('league_shape')=='EFL Championship' and g['competition_id']==CHAMPIONSHIP_FIXTURE_COMP_ID) else 0
            # Within the already locked league/current season, identity overlap only breaks ties.
            score=(overlap,legacy_hint,-abs(sh),-g['competition_id'])
            scored.append((score,g,sh,mapped))
            candidate_debug.append({
'competition_id':g['competition_id'],'season_start':g['season_start'],
                                    'league_shape':g.get('league_shape'),'shift':sh,'mapped_clubs':sorted(mapped),
                                    'identity_overlap':overlap}
)
        except Exception as e:
            candidate_debug.append({
'competition_id':g['competition_id'],'season_start':g['season_start'],
                                    'league_shape':g.get('league_shape'),'error':str(e)}
)
            continue
    if not scored:
        raise RuntimeError('Supported current-season fixture candidates were found, but none could be linked cleanly to English club identities in this save.')
    scored.sort(key=lambda x:x[0],reverse=True)
    score,g,shift,mapped=scored[0]
    rows=g['rows'];
gw_info=_normalize_gameweeks(rows);
spec=SUPPORTED_LEAGUES[g['league_shape']]
    info={
'competition_id':g['competition_id'],'season_start':g['season_start'],'fixture_to_club_shift':shift,
          'candidate_groups':len(groups),'exact_candidate_groups':len(exact_all),
          'current_season_candidate_groups':len(exact),'current_season_candidates':candidate_debug[:24],
          'requested_league':requested,'selection_mode':selection_mode,
          'rich_name_overlap':score[0],'competition':g['league_shape'],'competition_code':spec['code'],
          'team_count':spec['teams'],'total_gameweeks':spec['rounds'],'fixture_count':spec['fixtures'],
          'gameweek_relabels':gw_info['raw_round_relabels'],'calendar_gameweek_windows':gw_info['calendar_windows'],
          'calendar_reassigned_count':gw_info['calendar_reassigned_count'],
          'calendar_reassigned_examples':gw_info['calendar_reassigned_examples']}

    return rows,info

def scan_clubs(db: bytes) -> dict[int, Club]:
    # Scan only FFFFFFFF anchors, then validate the proven entity-head shape 22 bytes before name length.
    out: dict[int,Club] = {
}

    for m in re.finditer(b'\xff\xff\xff\xff', db):


=== PY contains both supported English leagues ===
index=9359

e':league_name}
)
    return groups


def select_championship_fixtures(fix:bytes,clubs:dict[int,Club],expected_names:set[str]|None=None,
                                 expected_league:str|None=None)->tuple[list[dict[str,Any]],dict[str,Any]]:
    """Select the current supported domestic league without trusting a fixed FM competition id.

    Manual league selection is a HARD constraint.  Auto detection never allows old retained
    match-detail from an earlier season/division to drag the save back into that old league.
    It first restricts candidates to the newest season present in the fixture calendar.
    """
    groups=scan_fixture_groups(fix)
    exact_all=[g for g in groups if g['exact']]
    if not exact_all:
        near=sorted(groups,key=lambda g:(abs(len(g['teams'])-24),-g['season_start']))[:10]
        summary=';
 '.join(f"comp={
g['competition_id']}
 season={
g['season_start']}
 fixtures={
len(g['rows'])}
 teams={
len(g['teams'])}
 gws={
len(g['gws'])}
 shape={
g.get('league_shape')}
" for g in near)
        raise RuntimeError('No supported full league season could be identified. Candidates: '+summary)

    requested=expected_league if expected_league in SUPPORTED_LEAGUES else None
    if requested:
        exact=[g for g in exact_all if g.get('league_shape')==requested]
        if not exact:
            raise RuntimeError(f'Manual league selection was {
requested}
, but no complete {
requested}
 season exists in this save. The selection will not be overridden.')
        newest=max(g['season_start'] for g in exact)
        exact=[g for g in exact if g['season_start']==newest]
        selection_mode='user_preference_locked'
    else:
        newest=max(g['season_start'] for g in exact_all)
        exact=[g for g in exact_all if g['season_start']==newest]
        shapes={
g.get('league_shape') for g in exact}

        if len(shapes)>1:
            summary=', '.join(sorted(str(x) for x in shapes))
            raise RuntimeError(f'Current season {
newest}
/{
str(newest+1)[-2:]}
 contains both supported English leagues ({
summary}
). Historical retained-match identity is deliberately ignored because a promoted/relegated human club may have old division data. Choose the current league once;
 the choice will be locked for this import.')
        selection_mode='auto_latest_season'

    scored=[]
    candidate_debug=[]
    for g in exact:
        try:
            sh=derive_fixture_to_club_shift(g['teams'],clubs,expected_names if requested else None)
            mapped={
normalize_club_name(clubs[tid-sh].short or clubs[tid-sh].name) for tid in g['teams'] if tid-sh in clubs}

            overlap=len(mapped & expected_names) if (requested and expected_names) else 0
            legacy_hint=1 if (g.get('league_shape')=='EFL Championship' and g['competition_id']==CHAMPIONSHIP_FIXTURE_COMP_ID) else 0
            # Within the already locked league/current season, identity overlap only breaks ties.
            score=(overlap,legacy_hint,-abs(sh),-g['competition_id'])
            scored.append((score,g,sh,mapped))
            candidate_debug.append({
'competition_id':g['competition_id'],'season_start':g['season_start'],
                                    'league_shape':g.get('league_shape'),'shift':sh,'mapped_clubs':sorted(mapped),
                                    'identity_overlap':overlap}
)
        except Exception as e:
            candidate_debug.append({
'competition_id':g['competition_id'],'season_start':g['season_start'],
                                    'league_shape':g.get('league_shape'),'error':str(e)}
)
            continue
    if not scored:
        raise RuntimeError('Supported current-season fixture candidates were found, but none could be linked cleanly to English club identities in this save.')
    scored.sort(key=lambda x:x[0],reverse=True)
    score,g,shift,mapped=scored[0]
    rows=g['rows'];
gw_info=_normalize_gameweeks(rows);
spec=SUPPORTED_LEAGUES[g['league_shape']]
    info={
'competition_id':g['competition_id'],'season_start':g['season_start'],'fixture_to_club_shift':shift,
          'candidate_groups':len(groups),'exact_candidate_groups':len(exact_all),
          'current_season_candidate_groups':len(exact),'current_season_candidates':candidate_debug[:24],
          'requested_league':requested,'selection_mode':selection_mode,
          'rich_name_overlap':score[0],'competition':g['league_shape'],'competition_code':spec['code'],
          'team_count':spec['teams'],'total_gameweeks':spec['rounds'],'fixture_count':spec['fixtures'],
          'gameweek_relabels':gw_info['raw_round_relabels'],'calendar_gameweek_windows':gw_info['calendar_windows'],
          'calendar_reassigned_count':gw_info['calendar_reassigned_count'],
          'calendar_reassigned_examples':gw_info['calendar_reassigned_examples']}

    return rows,info

def scan_clubs(db: bytes) -> dict[int, Club]:
    # Scan only FFFFFFFF anchors, then validate the proven entity-head shape 22 bytes before name length.
    out: dict[int,Club] = {
}

    for m in re.finditer(b'\xff\xff\xff\xff', db):
        ff=m.start();
 len_at=ff+22
        if len_at < 39 or len_at+8 >= len(db): continue
        try:
            n1=u32(db,len_at)
            if not 3<=n1<=64: continue
            ns=len_at+4
            name=db[ns:ns+n1].decode('utf-8')
            sa=ns+n1;
 n2=u32(db,sa)
            if not 2<=n2<=32: continue
            short=db[sa+4:sa+4+n2].decode('utf-8')
            if not name or not short or any(ord(c)<32 for c in name+short): continue
            ff2=u32(db,len_at-22)
            if ff2 != 0xFFFFFFFF: continue
            eid=u32(db,len_at-39);
 uid=u32(db,len_at-35);
 uid2=u32(db,len_at-31)
            zero=db[len_at-27]
            nation1=u32(db,len_at-14);
 loc=u32(db,len_at-18);
 nation3=u32(db,len_at-26)
            if zero!=0 or eid in (0,0xFFFFFFFF) or uid in (0,0xFFFFFFFF) or uid!=uid2: continue
            if not (nation1==nation3 or nation1==loc or nation3==loc): continue
            if any(x==0 or x>10000 for x in (nation1,loc,nation3)): continue
            out[eid]=Club(eid,uid,name,short,nation1)
        except Exception:
            continue
    return out


def derive_fixture_to_club_shift(team_ids: set[int], clubs: dict[int,Club], expected_names:set[str]|None=None) -> int:
    # Team ids and club eids are different namespaces. Derive their constant offset from this save.
    candidates=collections.Counter()
    english={
eid for eid,c in clubs.items() if c.nation_id==139}

    # Fast likely range;
 choose a shift that maps all 24 fixture-team ids to English clubs.
    for shift in range(-512,513):
        n=sum(1 for tid in team_ids if tid-shift in english)
        if n: candidates[shift]=n
    if not candidates: raise RuntimeError('Could not link fixture team ids to club entity ids')
    maxn=max(candidates.values())
    tied=[sh for sh,n in candidates.items() if n==maxn]
    if expected_names:
        def score(sh):
            mapped={
normalize_club_name(clubs[tid-sh].short or clubs[tid-sh].name) for 

=== PY preferred_league ===
index=94290

pton' else 0)}
 for c in clubs}

    for f in fixtures:
        if f['status']!='played':continue
        h,a=f['home'],f['away'];
hg,ag=int(f['home_score']),int(f['away_score'])
        sh,sa=st[h],st[a]
        for x in (sh,sa):x['P']+=1
        sh['GF']+=hg;
sh['GA']+=ag;
sa['GF']+=ag;
sa['GA']+=hg
        if hg>ag:sh['W']+=1;
sa['L']+=1
        elif ag>hg:sa['W']+=1;
sh['L']+=1
        else:sh['D']+=1;
sa['D']+=1
    rows=[]
    for x in st.values():
        x['GD']=x['GF']-x['GA'];
x['Pts']=x['W']*3+x['D']+x['adjustment'];
rows.append(x)
    rows.sort(key=lambda x:(x['Pts'],x['GD'],x['GF']),reverse=True)
    for i,x in enumerate(rows,1):x['position']=i
    return rows


def build_star_teams(players:list[dict[str,Any]],latest_gw:int)->dict[str,dict[str,Any]]:
    out={
}

    formations=[]
    for d in range(3,6):
        for m in range(2,6):
            f=10-d-m
            if 1<=f<=3:formations.append((d,m,f))
    for gw in range(1,max(1,latest_gw)+1):
        bypos={
pos:sorted((p for p in players if p.get('pos')==pos),key=lambda p:(int(p.get('weekly_points',{
}
).get(str(gw),0)),p.get('fantasy_points',0)),reverse=True) for pos in ('GK','DEF','MID','FWD')}

        best=None
        for d,m,f in formations:
            picks=bypos['GK'][:1]+bypos['DEF'][:d]+bypos['MID'][:m]+bypos['FWD'][:f]
            if len(picks)!=11 or len({
p['id'] for p in picks}
)!=11:continue
            pts=sum(int(p.get('weekly_points',{
}
).get(str(gw),0)) for p in picks)
            if best is None or pts>best[0]:best=(pts,[p['id'] for p in picks],{
'GK':1,'DEF':d,'MID':m,'FWD':f}
)
        if best:out[str(gw)]={
'points':best[0],'ids':best[1],'formation':best[2]}

    return out

def _build_payload(save_path:Path,fingerprint:str,db,fix:bytes,include_rich:bool=True)->dict[str,Any]:
    all_clubs=scan_clubs(db)
    rich_raw=rich_extract_named(save_path,all_clubs) if include_rich else []
    comp_counts=collections.Counter(m.get('competition') for m in rich_raw if m.get('competition'))
    requested_league=preferred_league if preferred_league in SUPPORTED_LEAGUES else None
    preselect_rich=[m for m in rich_raw if (not requested_league or m.get('competition')==requested_league)]
    expected_names={
normalize_club_name(m[k]) for m in preselect_rich for k in ('home','away')}
 if (requested_league and preselect_rich) else None
    fixtures,fixture_info=select_championship_fixtures(fix,all_clubs,expected_names,requested_league)
    expected_league=fixture_info['competition']
    rich_raw=[m for m in rich_raw if m.get('competition')==expected_league]
    team_ids={
x for f in fixtures for x in (f['home_tid'],f['away_tid'])}

    shift=fixture_info['fixture_to_club_shift']
    selected={
tid-shift:all_clubs[tid-shift] for tid in team_ids}

    # Second retained-match pass, AFTER league selection. This cannot change the chosen
    # competition or club set;
 it only attempts to recover richer player history for the
    # selected clubs (notably some Premier League saves).
    selected_aliases=selected_rich_team_aliases(selected)
    selected_names={
normalize_club_name(c.short or c.name) for c in selected.values()}

    post_raw=[]
    for i,name in enumerate(rich_names):
        p=Path(f'/tmp/rich_{
i}
.bin')
        if not p.exists(): continue
        buf=p.read_bytes()
        for m in _rich_extract_member(buf,selected_aliases,name):
            if m.get('competition')!=fixture_info['competition']: continue
            if normalize_club_name(m.get('home','')) not in selected_names or normalize_club_name(m.get('away','')) not in selected_names: continue
            post_raw.append(m)
        p.unlink(missing_ok=True)
    post_recovered=0
    if post_raw:
        combined=[];
seen_post=set()
        for m in list(rich_raw)+post_raw:
            key=(m.get('competition'),m.get('home'),m.get('away'),m.get('home_score'),m.get('away_score'),
                 tuple(x.get('player_id') for x in m.get('home_players',[])),tuple(x.get('player_id') for x in m.get('away_players',[])))
            if key in seen_post: continue
            seen_post.add(key);
combined.append(m)
        post_recovered=max(0,len(combined)-len(rich_raw));
rich_raw=combined
    fixture_name={
tid:normalize_club_name(selected[tid-shift].short or selected[tid-shift].name) for tid in team_ids}

    for i,f in enumerate(sorted(fixtures,key=lambda x:(x['gameweek'],x['date'],x['home_tid'],x['away_tid'])),1):
        f['fixture_id']=i;
f['home']=fixture_name[f['home_tid']];
f['away']=fixture_name[f['away_tid']]
    results=scan_completed_results(db,fixtures)
    # First recover senior squads independently. If separate retained match-detail files are
    # missing, use grounded completed-result anchors inside game_db.dat to recover old player rows.
    squads,squad_diag=scan_first_team_squads(db,selected,rich_raw)
    bad_squads={
eid:len(vals) for eid,vals in squads.items() if not (12<=len(vals)<=45)}

    if squad_diag.get('missing_club_eids') or bad_squads:
        raise RuntimeError(f"Current first-team squad decode failed safely;
 missing={
squad_diag.get('missing_club_eids',[])}
 invalid_sizes={
bad_squads}
. Import blocked rather than guessing from match history.")
    game_db_rich,game_db_rich_diag=recover_game_db_rich_matches(
        db,fixtures,selected,squads,shift,fixture_info['competition'])
    if game_db_rich:
        existing={
(normalize_club_name(m.get('home','')),normalize_club_name(m.get('away','')),
                   int(m.get('home_score') or 0),int(m.get('away_score') or 0)) for m in rich_raw}

        for m in game_db_rich:
            k=(normalize_club_name(m.get('home','')),normalize_club_name(m.get('away','')),
               int(m.get('home_score') or 0),int(m.get('away_score') or 0))
            if k not in existing:
                rich_raw.append(m);
existing.add(k)
        # v68: recovered match participation never changes current squad membership.
    target_eids={
p for vals in squads.values() for p in vals}

    for m in rich_raw:
        for key in ('home_players','away_players'):
            for r in m.get(key,[]):target_eids.add(int(r['player_id']))
    people=bind_target_people(db,target_eids)
    players,unresolved,ambiguous=build_players(squads,selected,people,rich_raw)
    # v72: ambiguity is evidence, not permission to guess and not a reason to lose
    # the rest of an otherwise valid league import. Ambiguous people are absent from
    # `players` (therefore cannot be selected) and remain in `ambiguous` for debugging
    # and future current-database-only resolver paths.
    pbyeid={
int(p['pid']):p for p in players}

    rich_matches=join_rich_matches(rich_raw,fixtures,pbyeid) if rich_raw else []
    _rich_fixture_ids={
int(m.get('fixture_id') or 0) for m in rich_matches}

    _coverage={
}

    for _gw in sorted({
int(f.get('gameweek') or 0) for f in fixtures if int(f.get('gameweek') or 0)>0}
):
        _pf=[f for f in fixtures if int(f.get('gameweek') or 0)==_gw and f.get('status')=='played']
        _coverage[str(_gw)]={
'played_fixtures':len(_

=== PY league_preference ===
index=-1
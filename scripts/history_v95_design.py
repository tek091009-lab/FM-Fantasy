# v95 design marker
# Recover unresolved retained match sides from an accumulated roster built only from
# player cohorts belonging to already accepted authoritative historical fixtures.
# Safety policy: at least two confirmed cohorts for the club, >=8 shared players,
# >=4 of them observed in two or more confirmed cohorts, >=44% side coverage,
# clear margin over the next club, then exact score + unique unused authoritative fixture.
V95_POLICY = 'confirmed-historical-roster-exact-fixture-v1'

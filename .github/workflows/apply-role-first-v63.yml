name: Apply v6.4 live fix
on:
  push:
    paths:
      - '.github/workflows/apply-role-first-v63.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply v6.4 combined production patch
        run: python scripts/upgrade_v64_livefix.py
      - name: Commit production bundle
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add app/
          git diff --cached --quiet && exit 0
          git commit -m "Apply v6.4 pricing position and history recovery"
          git push
